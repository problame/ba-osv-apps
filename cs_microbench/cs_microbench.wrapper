#!/bin/bash

# a helper to execute microbenchmarks using libfakeosv and niced on a Linux system

cd "$(dirname $0)"
export LD_LIBRARY_PATH=$(readlink -f ../../libfakeosv)

exec nice -n -20 ./cs_microbench $@

